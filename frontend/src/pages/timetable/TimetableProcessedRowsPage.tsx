import { TimetableSectionFocusPage } from './TimetableSectionFocusPage';

export default function TimetableProcessedRowsPage() {
  return (
    <TimetableSectionFocusPage
      sectionId="timetable-processed-section"
      title="Processed Rows View"
      description="Focused on processed import rows, outcomes, and booking CRUD after import processing."
    />
  );
}
